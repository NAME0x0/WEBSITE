{
    "name": "personal-website",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
      "build": "mkdir -p dist && cp -r *.html *.css *.js src dist/",
      "start": "serve .",
      "test": "echo \"No tests specified\""
    },
    "dependencies": {
      "serve": "^14.2.1"
    },
    "devDependencies": {
      "eslint": "^8.57.0"
    }
  }